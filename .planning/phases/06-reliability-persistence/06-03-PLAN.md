---
plan: "06-03"
phase: "06"
type: "implementation"
wave: 3
status: "planned"
depends_on: ["06-01", "06-02"]
requirements: ["REQ-6.3"]
files_modified: ["ui/app.js", "index.html", "services/pdfService.js", "tests/e2e/persistence.spec.js"]
autonomous: false
title: "Resume Logic & UI Recovery Integration"
description: "Enable the application to detect and recover from interrupted sessions, providing a seamless 'Resume' experience for the user."
must_haves:
  truths:
    - "A 'Resume Session' prompt appears if the user refreshes during an active batch."
  artifacts:
    - "tests/e2e/persistence.spec.js"
  key_links:
    - "app.js calls pdfService.getInterruptedJobs()"
---

# Plan: Resume Logic & UI Recovery Integration (06-03)

## 🛠️ Tasks

<task id="1" type="auto">
<name>Implement Recovery UI & Logic in app.js</name>
<files>
- `ui/app.js`
- `index.html`
</files>
<action>
1. Add a recovery banner template to `index.html` (hidden by default).
2. Update `app.js` `initEngine` to check `pdfService.getInterruptedJobs()`.
3. If jobs found, display the recovery banner.
4. If 'Resume' is clicked, update the global `fileQueue` with recovered files and trigger `processQueue`.
5. If 'Discard' is clicked, clear the IndexedDB job and hide the banner.
</action>
<verify>
<automated>
npx playwright test tests/e2e/persistence.spec.js --reporter=line
</automated>
</verify>
<done>
The UI correctly detects interrupted sessions and provides a working recovery path.
</done>
</task>

<task id="2" type="checkpoint:human-verify">
<name>Verify End-to-End Recovery Flow</name>
<files>N/A</files>
<action>
Manual verification of the full recovery cycle:
1. Open the PWA.
2. Drag 5 large PDFs (or simulated large files).
3. Wait for 2 to finish processing.
4. Refresh the page manually (F5).
5. Verify the "Resume Session" banner appears.
6. Click "Resume".
7. Verify the Bento Grid populates with previous state (2 success, 3 pending).
8. Verify the remaining 3 files process correctly.
</action>
<verify>
User confirms behavior by typing "approved".
</verify>
<done>
Phase 06-03 is complete when the recovery flow is fully functional and passes both automated and manual verification.
</done>
</task>

<done>
All tasks in Phase 06 are completed.
</done>
