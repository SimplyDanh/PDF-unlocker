---
plan: "05-03"
name: "Security UX & Integration"
wave: 3
depends_on: ["05-02"]
requirements: ["REQ-5.3", "REQ-5.4"]
must_haves:
  truths:
    - "User can view the SHA-256 hash of a processed file on its UI card."
    - "User can access a local-only audit trail showing history of processed files."
  artifacts:
    - path: "ui/app.js"
      provides: "Audit viewer logic and hash rendering"
    - path: "ui/styles.css"
      provides: "Styles for the Audit Log and security indicators"
  key_links:
    - from: "ui/app.js"
      to: "services/auditService.js"
      via: "event listener for 'View Log' button"
---

# 05-03: Security UX & Integration

## Tasks

<task>
<read_first>
- "ui/app.js"
- "ui/styles.css"
</read_first>
<action>
Update the `FileCard` component in `ui/app.js`. Add a signature area at the bottom of the card that displays the SHA-256 hash. Implement a "Verified" tooltip that appears on hover. In `ui/styles.css`, define styles for the signature text (monospaced, small, subtle) and the verified badge (green glow).
</action>
<acceptance_criteria>
- Processed file cards show a `Signature: {hash...}` section.
- Hovering over the signature shows "Cryptographically Verified".
- Text uses `--font-mono` and a subtle opacity.
</acceptance_criteria>
</task>

<task>
<read_first>
- "index.html"
- "ui/app.js"
- "services/auditService.js"
</read_first>
<action>
Implement the "Audit Trail" view. Add a button to the "About" modal (or a new footer icon) that opens a modal showing a table of past events. Fetch data using `AuditService.getLogs()` (implement this method in `auditService.js`). Columns: Timestamp, Action, Filename, Hash, Result.
</action>
<acceptance_criteria>
- A new modal "Security Audit Trail" is accessible from the UI.
- The table correctly reflects data stored in IndexedDB.
- Individual entries include a "Copy Hash" button.
</acceptance_criteria>
</task>

<task>
<read_first>
- "tests/e2e/security_ux.spec.js"
</read_first>
<action>
Create `tests/e2e/security_ux.spec.js`. Test flow: 
1. Drag and drop file.
2. Verify "Verified" indicator appears on card.
3. Open Audit Trail.
4. Confirm the new entry exists with matching hash.
</action>
<acceptance_criteria>
- `npx playwright test tests/e2e/security_ux.spec.js --reporter=line` passes.
- All UX elements respond as expected to user interactions.
</acceptance_criteria>
</task>
