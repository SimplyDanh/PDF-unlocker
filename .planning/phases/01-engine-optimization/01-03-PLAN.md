---
phase: 01-engine-optimization
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [ui/app.js, sw.js]
autonomous: true
requirements: [FR-3, FR-4, UI-1, UI-2]
must_haves:
  truths:
    - "Drop zone displays 'Loading engine...' during bootstrap"
    - "Drop zone becomes active only after Worker reports 'READY'"
    - "Versioned assets (.wasm, unpkg .js) are served Cache-First"
  artifacts:
    - path: "ui/app.js"
      provides: "UI state management for engine loading"
    - path: "sw.js"
      provides: "Cache-First strategy for versioned assets"
  key_links:
    - from: "ui/app.js"
      to: "services/pdfService.js"
      via: "status and ready event listener"
      pattern: "READY"
    - from: "sw.js"
      to: "Cache Storage"
      via: "event.respondWith(caches.match(event.request))"
      pattern: "unpkg\\.com"
---

<objective>
Enhance user feedback during engine initialization and optimize asset delivery via the Service Worker.

Purpose: Provide a clear loading state and ensure near-instant load times for repeated visits.
Output: Responsive "Loading" UI and optimized Cache-First Service Worker.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-RESEARCH.md
@ui/app.js
@sw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Engine Loading UI State</name>
  <files>ui/app.js</files>
  <action>
    Modify ui/app.js to display a loading state during engine initialization.
    - Set initial drop zone status to "Loading engine..." with a sub-status "Preparing local environment".
    - Disable file input and drop zone interactions until the Worker signals 'READY'.
    - Transition the UI to "Awaiting Document" once the 'READY' message is received from the worker.
  </action>
  <verify>
    <automated>grep "Loading engine" ui/app.js</automated>
  </verify>
  <done>UI properly reflects the engine loading state and prevents early interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Update Service Worker Cache Strategy</name>
  <files>sw.js</files>
  <action>
    Update the sw.js fetch listener to implement a Cache-First strategy for versioned dependencies.
    - Identify versioned assets from unpkg.com (e.g., URLs containing '@0.3.0' or specific library versions).
    - For these assets, serve from cache first, then fall back to the network and update the cache.
    - Maintain the Network-First strategy for non-versioned assets (like index.html) to ensure updates are fetched properly.
  </action>
  <verify>
    <automated>grep "unpkg.com" sw.js</automated>
  </verify>
  <done>Service Worker provides optimized loading for cached WASM and engine JS.</done>
</task>

</tasks>

<verification>
- Manually reload the page and verify the "Loading engine..." message is visible before transitioning to "Awaiting Document".
- Verify Service Worker behavior in Chrome DevTools (Application -> Service Worker -> Network).
- Reload multiple times and confirm versioned assets are served from the Service Worker cache with (ServiceWorker) status in the Size column.
</verification>

<success_criteria>
- The UI shows clear initialization feedback.
- Assets are served Cache-First for versioned dependencies, reducing load time.
- Interactions are gated until the WASM engine is ready.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-optimization/01-03-SUMMARY.md`
</output>
