---
phase: 01-engine-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/pdfWorker.js, services/pdfService.js, tests/pdfWorker.test.js]
autonomous: true
requirements: [FR-1, SEC-1, TST-1]
must_haves:
  truths:
    - "WASM engine is completely isolated from the main thread"
    - "Main UI thread remains responsive (60fps) during heavy processing"
    - "Worker logic is verified by unit tests"
  artifacts:
    - path: "services/pdfWorker.js"
      provides: "WASM runtime & processing logic"
    - path: "services/pdfService.js"
      provides: "Main thread Proxy for Worker communication"
    - path: "tests/pdfWorker.test.js"
      provides: "Worker logic verification"
  key_links:
    - from: "services/pdfService.js"
      to: "services/pdfWorker.js"
      via: "new Worker('services/pdfWorker.js')"
      pattern: "new Worker"
---

<objective>
Migrate the QPDF WASM engine from the main thread to a dedicated Web Worker to ensure UI responsiveness.

Purpose: Offload resource-intensive PDF processing to prevent main-thread blocking.
Output: Working PDF processing engine running in an isolated Worker thread.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/01-RESEARCH.md
@services/pdfService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services/pdfWorker.js</name>
  <files>services/pdfWorker.js</files>
  <action>
    Extract the core WASM initialization and processing logic from pdfService.js into a new services/pdfWorker.js file.
    - Implement a structured message protocol: { type: 'init' }, { type: 'process', file: ArrayBuffer, name: string }.
    - Worker should respond with: { type: 'ready' }, { type: 'status', state: string, main: string, sub: string }, { type: 'success', blob: ArrayBuffer, name: string }, { type: 'error', main: string, sub: string }.
    - Use Transferable Objects for zero-copy data transfer of ArrayBuffers.
    - Import scripts from unpkg (qpdf.js) using importScripts.
  </action>
  <verify>
    <automated>ls services/pdfWorker.js</automated>
  </verify>
  <done>Worker logic implemented with message listeners and Transferable support.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor services/pdfService.js to Proxy</name>
  <files>services/pdfService.js</files>
  <action>
    Refactor services/pdfService.js to act as a lightweight Proxy/Client.
    - Instantiate the Worker and handle message routing.
    - Maintain isProcessing state on the main thread based on Worker messages.
    - Proxy processFile calls to the Worker using postMessage.
    - Map Worker 'status', 'success', and 'error' messages back to the existing onStatus callback.
  </action>
  <verify>
    <automated>npm test tests/pdfService.test.js</automated>
  </verify>
  <done>pdfService.js successfully proxies requests to the Worker and handles callbacks.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/pdfWorker.test.js (Wave 0 Gap)</name>
  <files>tests/pdfWorker.test.js</files>
  <action>
    Create a unit test for the Worker logic in isolation.
    - Use Vitest to mock the Worker environment or handle the file as a standard JS module for logic testing.
    - Verify that the Worker correctly processes a mock ArrayBuffer and emits the appropriate success/error messages.
  </action>
  <verify>
    <automated>npm test tests/pdfWorker.test.js</automated>
  </verify>
  <done>Worker logic verified by unit tests.</done>
</task>

</tasks>

<verification>
- Run unit tests for pdfService and pdfWorker.
- Manually verify that uploading a PDF still works and successfully unlocks the file.
- Verify in browser DevTools that the 'qpdf.wasm' is now loaded by a Worker, not the main thread.
</verification>

<success_criteria>
- PDF processing is offloaded to the worker thread.
- Zero-copy (Transferable Objects) is used for data transfer.
- Existing UI callbacks (onStatus) are correctly triggered by Worker events.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-optimization/01-01-SUMMARY.md`
</output>
