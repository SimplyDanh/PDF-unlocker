---
phase: 07-automation-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [services/diagnosticsService.js, services/persistenceService.js, ui/app.js]
autonomous: true
requirements: [REQ-7.4]
must_haves:
  truths:
    - "Application tracks worker initialization time, processing duration, and memory usage per file."
    - "Crash statistics and engine failures are recorded locally for diagnosis."
    - "Performance metrics persist across sessions in IndexedDB."
  artifacts:
    - path: "services/diagnosticsService.js"
      provides: "Telemetry collection and performance monitoring"
    - path: "services/persistenceService.js"
      provides: "IndexedDB store for performance metrics"
  key_links:
    - from: "services/pdfService.js"
      to: "services/diagnosticsService.js"
      via: "Method calls for event tracking"
    - from: "services/diagnosticsService.js"
      to: "services/persistenceService.js"
      via: "Metric storage"
---

<objective>
Implement performance instrumentation and diagnostic telemetry to monitor engine health, track resource usage (memory/CPU), and provide locally persistent metrics for performance auditing and troubleshooting.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@services/persistenceService.js
@ui/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Performance Diagnostics Service</name>
  <files>services/diagnosticsService.js</files>
  <action>
    Create `services/diagnosticsService.js` to handle performance telemetry.
    - Implement methods to record events: `recordWorkerStart()`, `recordProcessComplete(fileSize, duration, memory)`, `recordError(context, error)`.
    - Use `performance.now()` for high-resolution timing.
    - Capture `performance.memory` (if available in browser) during processing peaks.
    - Aggregate statistics: Average processing time per MB, total files processed, error rate.
    - Export a revealing module pattern or class-based service.
  </action>
  <verify>
    <automated>npx vitest tests/perf.test.js</automated>
  </verify>
  <done>Diagnostics service is created and capable of tracking granular performance events.</done>
</task>

<task type="auto">
  <name>Task 2: Persistent Metrics Storage</name>
  <files>services/persistenceService.js, services/diagnosticsService.js, ui/app.js</files>
  <action>
    1. Update `services/persistenceService.js`:
       - Add a `metrics` object store to IndexedDB (increment DB version to 3).
       - The store should hold timestamped metric records.
       - Implement `saveMetric(event)` and `getRecentMetrics(limit)` methods.
    2. Update `services/diagnosticsService.js` to call `persistenceService.saveMetric` for all major events.
    3. Integrate into `ui/app.js`:
       - Ensure `diagnosticsService` is initialized.
       - Connect `pdfService` events (success/error/worker start) to the diagnostics service.
       - (Optional) Add a "System Diagnostics" view or console output for the user.
  </action>
  <verify>
    <automated>npx vitest tests/persistenceService.test.js</automated>
  </verify>
  <done>Performance metrics are persistently stored and linked to real-world engine operations.</done>
</task>

</tasks>

<verification>
1. Process several PDF files of varying sizes.
2. Open Browser DevTools -> Application -> IndexedDB -> `pdf_unlocker_db` -> `metrics`.
3. Verify that entries exist for worker starts and processing completions with realistic durations.
4. Simulate an error (e.g., invalid file) and verify an error metric is recorded.
5. Check if the average processing speed can be calculated from the recorded data.
</verification>

<success_criteria>
- Zero impact on processing performance from telemetry collection.
- Persistent storage of at least: worker init time, process duration, file size, and error context.
- Clean separation between diagnostics logic and core business logic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-automation-accessibility/07-03-SUMMARY.md`
</output>
