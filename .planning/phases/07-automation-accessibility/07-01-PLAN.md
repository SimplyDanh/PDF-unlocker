---
phase: 07-automation-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/generateSri.js, playwright.config.js, .github/workflows/test.yml]
autonomous: true
requirements: [REQ-7.1, REQ-7.2]
must_haves:
  truths:
    - "SRI registry automatically includes all local JS, CSS, and WASM assets without hardcoding."
    - "CI fails if any integrity hash in index.html or pdfWorker.js is out of sync."
    - "Playwright runs tests across Chromium, Firefox, and WebKit in GitHub Actions."
  artifacts:
    - path: "scripts/generateSri.js"
      provides: "Dynamic SRI generation using glob patterns"
    - path: "playwright.config.js"
      provides: "Multi-browser test configuration"
    - path: ".github/workflows/test.yml"
      provides: "Automated SRI verification and E2E testing"
  key_links:
    - from: "scripts/generateSri.js"
      to: "scripts/sri-hashes.json"
      via: "File write"
    - from: ".github/workflows/test.yml"
      to: "scripts/verifySri.js"
      via: "CI command execution"
---

<objective>
Automate the SRI pipeline to ensure security integrity remains up-to-date and expand the CI pipeline to support multi-browser testing across all major engines.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@scripts/generateSri.js
@scripts/verifySri.js
@playwright.config.js
@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic SRI Generation Pipeline</name>
  <files>scripts/generateSri.js</files>
  <action>
    Refactor `scripts/generateSri.js` to replace the hardcoded `assets` array with dynamic file discovery.
    - Use `fs.readdirSync` recursively or a minimal glob-like implementation to find all `.js`, `.css`, and `.wasm` files in `ui/`, `services/`, and `assets/vendor/`.
    - Filter out unnecessary files (e.g., hidden files, non-asset files).
    - Ensure the script still writes to `scripts/sri-hashes.json`.
    - Update the script to handle paths relative to the project root correctly.
  </action>
  <verify>
    <automated>node scripts/generateSri.js && jq '.' scripts/sri-hashes.json</automated>
  </verify>
  <done>SRI registry is dynamically generated and contains hashes for all relevant project assets.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-Browser CI Configuration</name>
  <files>playwright.config.js, .github/workflows/test.yml</files>
  <action>
    1. Update `playwright.config.js` to include projects for `chromium`, `firefox`, and `webkit`.
    2. Set `trace: 'on-first-retry'` and `video: 'on-first-retry'` for CI debugging.
    3. Update `.github/workflows/test.yml`:
       - Add a step to run `node scripts/verifySri.js` after installing dependencies.
       - Update the test step to run `npx playwright test`.
       - Ensure all necessary browser engines are installed using `npx playwright install --with-deps`.
  </action>
  <verify>
    <automated>npx playwright test --list && node scripts/verifySri.js</automated>
  </verify>
  <done>Playwright is configured for multi-browser testing and the CI pipeline enforces SRI verification and E2E success.</done>
</task>

</tasks>

<verification>
1. Run `node scripts/generateSri.js` and verify `scripts/sri-hashes.json` contains more entries than before (now dynamic).
2. Run `node scripts/verifySri.js` and ensure it passes.
3. Modify one file (e.g., `ui/app.js`), run `node scripts/verifySri.js` WITHOUT running generator, it should fail.
4. Run generator, then verify, it should pass.
5. Run `npx playwright test` locally to confirm multi-browser projects are triggered.
</verification>

<success_criteria>
- SRI generator successfully identifies assets dynamically.
- SRI verification script is integrated into the CI pipeline.
- E2E tests pass across all configured browser engines (Chromium, Firefox, WebKit).
</success_criteria>

<output>
After completion, create `.planning/phases/07-automation-accessibility/07-01-SUMMARY.md`
</output>
