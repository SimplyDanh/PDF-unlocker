---
phase: 07-automation-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html, ui/app.js, ui/styles.css]
autonomous: true
requirements: [REQ-7.3]
must_haves:
  truths:
    - "Bento Grid cards are keyboard navigable using arrow keys (roving tabindex)."
    - "Theme HUD swatches are accessible via keyboard and have appropriate ARIA labels."
    - "All interactive elements have visible focus states and descriptive ARIA labels."
  artifacts:
    - path: "index.html"
      provides: "Semantic HTML and static ARIA landmarks"
    - path: "ui/app.js"
      provides: "Roving tabindex logic and dynamic ARIA updates"
    - path: "ui/styles.css"
      provides: "High-visibility focus rings and accessible hidden labels"
  key_links:
    - from: "ui/app.js"
      to: "index.html"
      via: "DOM manipulation and event listeners"
---

<objective>
Implement full accessibility parity across the application, focusing on ARIA compliance, keyboard navigation for the Bento Grid and Theme HUD, and a comprehensive accessibility audit of all interactive components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@index.html
@ui/app.js
@ui/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic HTML & ARIA Audit</name>
  <files>index.html, ui/app.js</files>
  <action>
    1. Update `index.html`:
       - Ensure all major sections use landmark roles (`main`, `header`, `footer`, `nav`).
       - Add `aria-label` or `aria-labelledby` to all icons and interactive buttons.
       - Ensure the `about-panel` has appropriate heading hierarchy and focus traps.
    2. Update `ui/app.js`:
       - Add `role="button"` and `tabindex="0"` to dynamically generated theme swatches.
       - Add `aria-label` to swatches indicating the theme name.
       - Ensure the `about-panel` implementation uses `aria-modal="true"`.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/securityUx.spec.js</automated>
  </verify>
  <done>All static and dynamic UI elements have appropriate semantic roles and ARIA attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Roving Tabindex & Keyboard Navigation</name>
  <files>ui/app.js, ui/styles.css</files>
  <action>
    1. Implement Roving Tabindex in `ui/app.js`:
       - For `bento-grid`: Allow arrow key (Up/Down/Left/Right) navigation between file cards. Only the currently focused card should have `tabindex="0"`, others `tabindex="-1"`.
       - For `theme-hud`: Allow Up/Down arrow key navigation between theme swatches.
       - Support 'Home' and 'End' keys for quick navigation.
    2. Update `ui/styles.css`:
       - Implement a consistent, high-visibility `:focus-visible` ring across the application.
       - Ensure no interactive element is missing a focus state.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/pwa_ui.spec.js</automated>
  </verify>
  <done>Interactive grids and HUDs are fully navigable via keyboard with intuitive arrow-key support.</done>
</task>

</tasks>

<verification>
1. Open the application and use only the keyboard to navigate.
2. Tab to the Theme HUD, expand it, and use arrow keys to switch themes.
3. Upload multiple files, and use arrow keys to navigate between cards in the Bento Grid.
4. Verify that 'Enter' or 'Space' triggers actions on focused elements.
5. Use an accessibility audit tool (like Axe-core) via Playwright to ensure 0 critical violations.
</verification>

<success_criteria>
- 100% ARIA parity for all interactive components.
- Full keyboard support (tabbing + arrow keys) for complex UI layouts.
- No accessibility violations found in standard audits.
</success_criteria>

<output>
After completion, create `.planning/phases/07-automation-accessibility/07-02-SUMMARY.md`
</output>
